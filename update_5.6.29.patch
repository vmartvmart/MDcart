diff --git a/CHANGELOG.json b/CHANGELOG.json
index de714f2..10c496f 100644
--- a/CHANGELOG.json
+++ b/CHANGELOG.json
@@ -213,5 +213,10 @@
     "version": "5.6.28",
     "fa": "امکان «حالت تست (Sandbox)» به درگاه بیت‌پی اضافه شد تا بشود قبل از وارد کردن اطلاعات واقعی، کل مسیر پرداخت را با محیط تستِ رسمی بیت‌پی امتحان کرد — بدون جابه‌جایی پول واقعی. برای تست: در «افزونه‌ها > درگاه‌های پرداخت > بیت‌پی»، کلید API تستی که بیت‌پی در مستندات رسمی خودش منتشر کرده را در فیلد «کلید API» وارد کنید و «حالت تست» را روشن کنید، سپس یک سفارش آزمایشی ثبت کنید. پیش از فعال شدن واقعی سایت، حتماً کلید API واقعی خودتان را جایگزین کرده و «حالت تست» را خاموش کنید.",
     "en": "Added a Sandbox (test) mode to the BitPay gateway so the full checkout flow can be tried against BitPay's own official test environment before entering real credentials - no real money moves. To test: under Extensions > Payment Gateways > BitPay, enter the test API key BitPay publishes in its own official documentation into the API Key field, turn on Sandbox Mode, then place a test order. Before going live, replace it with your real API key and turn Sandbox Mode back off."
+  },
+  {
+    "version": "5.6.29",
+    "fa": "روش پرداخت در صفحه تسویه‌حساب حالا مستقیم نمایش داده می‌شود: به‌جای اینکه مشتری اول دکمه‌ی «انتخاب» را بزند و بعد یک پنجره باز شود، همین که اطلاعات لازم آماده باشد (بعد از تکمیل اطلاعات شخصی، و در صورت نیاز به ارسال، بعد از انتخاب روش ارسال)، لیست درگاه‌های فعال همان‌جا توی صفحه نشان داده می‌شود و مشتری فقط یکی را انتخاب می‌کند.",
+    "en": "The payment method step on checkout now shows the active gateways directly on the page instead of requiring a \"Choose\" button click that opened a separate popup - as soon as the needed information is ready (after personal info is completed, and after a shipping method is chosen if the cart needs shipping), the list of active gateways appears inline and the customer just picks one."
   }
 ]
diff --git a/VERSION b/VERSION
index 030ab30..83e2d80 100644
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-5.6.28
\ No newline at end of file
+5.6.29
\ No newline at end of file
diff --git a/catalog/controller/checkout/payment_method.php b/catalog/controller/checkout/payment_method.php
index 1d6dbc3..fed1a26 100644
--- a/catalog/controller/checkout/payment_method.php
+++ b/catalog/controller/checkout/payment_method.php
@@ -47,6 +47,16 @@ class PaymentMethod extends \MDcart\System\Engine\Controller {
 
 		$data['language'] = $this->config->get('config_language');
 
+		// The payment methods list is now shown inline (no more "Choose" button
+		// + modal) - it fetches and renders automatically as soon as its
+		// dependencies are known, so the template needs to know whether
+		// shipping applies to this cart (to wait for a shipping method to be
+		// picked) and, on a fresh page load, whether it can fetch right away
+		// (e.g. the customer already completed checkout up to this point in
+		// this session, such as after a page refresh).
+		$data['has_shipping'] = $this->cart->hasShipping();
+		$data['ready'] = isset($this->session->data['customer']) && (!$this->cart->hasShipping() || isset($this->session->data['shipping_method']));
+
 		return $this->load->view('checkout/payment_method', $data);
 	}
 
diff --git a/catalog/language/fa/checkout/payment_method.php b/catalog/language/fa/checkout/payment_method.php
index 1848a58..4c35d19 100644
--- a/catalog/language/fa/checkout/payment_method.php
+++ b/catalog/language/fa/checkout/payment_method.php
@@ -9,6 +9,9 @@ $_['text_comments']          = 'افزودن یادداشت درباره سفا
 $_['text_agree']             = 'من <a href="%s" class="modal-link"><b>%s</b></a> را مطالعه کرده و می‌پذیرم';
 $_['text_success']           = 'موفق: روش پرداخت را تغییر دادید!';
 $_['text_comment']           = 'موفق: یادداشت اضافه شد!';
+$_['text_loading']           = 'در حال بارگذاری روش‌های پرداخت...';
+$_['text_select_shipping_first'] = 'پس از انتخاب روش ارسال، روش‌های پرداخت نمایش داده می‌شود.';
+$_['text_complete_info_first']   = 'پس از تکمیل اطلاعات شخصی، روش‌های پرداخت نمایش داده می‌شود.';
 
 // Entry
 $_['entry_payment_method']   = 'روش پرداخت را انتخاب کنید...';
diff --git a/catalog/language/fr-fr/checkout/payment_method.php b/catalog/language/fr-fr/checkout/payment_method.php
index cfd3acc..8be1904 100644
--- a/catalog/language/fr-fr/checkout/payment_method.php
+++ b/catalog/language/fr-fr/checkout/payment_method.php
@@ -9,6 +9,9 @@ $_['text_comments']          = 'Ajouter des commentaires sur votre commande';
 $_['text_agree']             = 'J\'ai lu et j\'accepte les <a href="%s" class="modal-link"><b>%s</b></a>';
 $_['text_success']           = 'Succès: Vous avez modifié le mode de paiement!';
 $_['text_comment']           = 'Succès: Commentaire ajouté!';
+$_['text_loading']           = 'Chargement des modes de paiement...';
+$_['text_select_shipping_first'] = 'Les modes de paiement s\'afficheront une fois le mode d\'expédition choisi.';
+$_['text_complete_info_first']   = 'Les modes de paiement s\'afficheront une fois vos informations personnelles complétées.';
 
 // Entrée
 $_['entry_payment_method']   = 'Choisir un mode de paiement...';
diff --git a/catalog/language/us/checkout/payment_method.php b/catalog/language/us/checkout/payment_method.php
index 4c42853..8a05911 100644
--- a/catalog/language/us/checkout/payment_method.php
+++ b/catalog/language/us/checkout/payment_method.php
@@ -9,6 +9,9 @@ $_['text_comments']          = 'Add Comments About Your Order';
 $_['text_agree']             = 'I have read and agree to the <a href="%s" class="modal-link"><b>%s</b></a>';
 $_['text_success']           = 'Success: You have changed payment method!';
 $_['text_comment']           = 'Success: Comment added!';
+$_['text_loading']           = 'Loading payment methods...';
+$_['text_select_shipping_first'] = 'Payment methods will appear once you choose a shipping method.';
+$_['text_complete_info_first']   = 'Payment methods will appear once you complete your personal info.';
 
 // Entry
 $_['entry_payment_method']   = 'Choose payment method...';
diff --git a/catalog/view/template/checkout/payment_method.twig b/catalog/view/template/checkout/payment_method.twig
index b11417c..3b47642 100644
--- a/catalog/view/template/checkout/payment_method.twig
+++ b/catalog/view/template/checkout/payment_method.twig
@@ -1,11 +1,8 @@
 <fieldset>
   <legend>{{ heading_title }}</legend>
-  <div class="input-group">
-    <span class="input-group-text"><i class="fa fa-credit-card"></i></span><input type="text" name="payment_method" value="{{ payment_method }}" placeholder="{{ entry_payment_method }}" id="input-payment-method" class="form-control" readonly/>
-    <button type="button" id="button-payment-methods" class="btn btn-primary">{{ button_choose }}</button>
-  </div>
+  <div id="payment-methods" class="mb-2"></div>
   <input type="hidden" name="code" value="{{ code }}" id="input-payment-code"/>
-  <div id="error-payment-method" class="invalid-feedback"></div>
+  <div id="error-payment-method" class="alert alert-danger d-none"></div>
 </fieldset>
 <br/>
 <div class="mb-2">
@@ -18,84 +15,68 @@
   </div>
 {% endif %}
 <script type="text/javascript"><!--
-$('#button-payment-methods').on('click', function() {
-    var element = this;
-
+// The payment method step used to be a read-only text field + a "Choose"
+// button that opened a modal with the actual list - the customer had to
+// click twice before ever seeing which gateways were available. It's now
+// shown as a plain inline list of the active gateways as soon as they can
+// be determined, and selecting one immediately saves it - no extra button,
+// no modal.
+window.checkoutHasShipping = {{ has_shipping ? 'true' : 'false' }};
+
+function paymentMethodsPlaceholder(text) {
+    $('#payment-methods').html('<div class="text-muted">' + text + '</div>');
+}
+
+window.loadPaymentMethods = function() {
     chain.attach(function() {
         return $.ajax({
             url: 'index.php?route=checkout/payment_method.getMethods&language={{ language }}',
             dataType: 'json',
             beforeSend: function() {
-                $(element).button('loading');
-            },
-            complete: function() {
-                $(element).button('reset');
+                paymentMethodsPlaceholder('{{ text_loading|escape('js') }}');
             },
             success: function(json) {
-                $('#input-payment-method').removeClass('is-invalid');
-                $('#error-payment-method').removeClass('d-block');
+                $('#error-payment-method').addClass('d-none').html('');
+
+                if (json['redirect']) {
+                    location = json['redirect'];
+                }
 
                 if (json['error']) {
-                    $('#input-payment-method').addClass('is-invalid');
-                    $('#error-payment-method').html(json['error']).addClass('d-block');
+                    $('#payment-methods').html('');
+                    $('#error-payment-method').removeClass('d-none').html(json['error']);
+
+                    return;
                 }
 
                 if (json['payment_methods']) {
-                    $('#modal-payment').remove();
-
-                    html = '<div id="modal-payment" class="modal">';
-                    html += '  <div class="modal-dialog modal-dialog-centered">';
-                    html += '    <div class="modal-content">';
-                    html += '      <div class="modal-header">';
-                    html += '        <h5 class="modal-title"><i class="fa fa-credit-card"></i> {{ text_payment_method|escape('js') }}</h5>';
-                    html += '        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>';
-                    html += '      </div>';
-                    html += '      <div class="modal-body">';
-                    html += '        <form id="form-payment-method">';
-                    html += '          <p>{{ text_payment|escape('js') }}</p>';
-
+                    var html = '';
                     var first = true;
+                    var method = $('#input-payment-code').val();
 
                     for (i in json['payment_methods']) {
-                        html += '<p><strong>' + json['payment_methods'][i]['name'] + '</strong></p>';
-
                         if (!json['payment_methods'][i]['error']) {
                             for (j in json['payment_methods'][i]['option']) {
-                                html += '<div class="form-check">';
-
                                 var code = i + '-' + j.replaceAll('_', '-');
+                                var checked = (json['payment_methods'][i]['option'][j]['code'] == method) || (!method && first);
 
-                                html += '<input type="radio" name="payment_method" value="' + json['payment_methods'][i]['option'][j]['code'] + '" id="input-payment-method-' + code + '"';
-
-                                var method = $('#input-payment-code').val();
-
-                                if ((json['payment_methods'][i]['option'][j]['code'] == method) || (!method && first)) {
-                                    html += ' checked';
-
+                                if (checked) {
                                     first = false;
                                 }
 
-                                html += '/>';
-                                html += '  <label for="input-payment-method-' + code + '">' + json['payment_methods'][i]['option'][j]['name'] + '</label>';
+                                html += '<div class="form-check">';
+                                html += '  <input type="radio" name="payment_method" value="' + json['payment_methods'][i]['option'][j]['code'] + '" id="input-payment-method-' + code + '" class="form-check-input"' + (checked ? ' checked' : '') + '/>';
+                                html += '  <label for="input-payment-method-' + code + '" class="form-check-label">' + json['payment_methods'][i]['option'][j]['name'] + '</label>';
                                 html += '</div>';
                             }
-                        } else {
-                            html += '<div class="alert alert-danger">' + json['payment_methods'][i]['error'] + '</div>';
                         }
                     }
 
-                    html += '          <div class="text-end">';
-                    html += '            <button type="submit" id="button-payment-method" class="btn btn-primary">{{ button_continue|escape('js') }}</button>';
-                    html += '          </div>';
-                    html += '        </form>';
-                    html += '      </div>';
-                    html += '    </div>';
-                    html += '  </div>';
-                    html += '</div>';
-
-                    $('body').append(html);
+                    $('#payment-methods').html(html);
 
-                    $('#modal-payment').modal('show');
+                    if ($('input[name=\'payment_method\']:checked').length) {
+                        $('input[name=\'payment_method\']:checked').trigger('change');
+                    }
                 }
             },
             error: function(xhr, ajaxOptions, thrownError) {
@@ -103,41 +84,30 @@ $('#button-payment-methods').on('click', function() {
             }
         });
     });
-});
-
-$(document).on('submit', '#form-payment-method', function(e) {
-    e.preventDefault();
-
-    var element = this;
+};
 
+// Selecting a gateway saves it immediately, matching how a radio list
+// behaves everywhere else on this store - no separate "continue" click.
+$(document).on('change', 'input[name=\'payment_method\']', function() {
     chain.attach(function() {
         return $.ajax({
             url: 'index.php?route=checkout/payment_method.save&language={{ language }}',
             type: 'post',
-            data: $('#form-payment-method').serialize(),
+            data: 'payment_method=' + encodeURIComponent($('input[name=\'payment_method\']:checked').val()),
             dataType: 'json',
             contentType: 'application/x-www-form-urlencoded',
-            beforeSend: function() {
-                $('#button-payment-method').button('loading');
-            },
-            complete: function() {
-                $('#button-payment-method').button('reset');
-            },
             success: function(json) {
                 if (json['redirect']) {
                     location = json['redirect'];
                 }
 
                 if (json['error']) {
-                    $('#alert').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa-solid fa-circle-exclamation"></i> ' + json['error'] + ' <button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>');
+                    $('#error-payment-method').removeClass('d-none').html(json['error']);
                 }
 
                 if (json['success']) {
-                    $('#alert').prepend('<div class="alert alert-success alert-dismissible"><i class="fa-solid fa-circle-check"></i> ' + json['success'] + ' <button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>');
-
-                    $('#modal-payment').modal('hide');
+                    $('#error-payment-method').addClass('d-none').html('');
 
-                    $('#input-payment-method').val($('input[name=\'payment_method\']:checked').parent().find('label').text());
                     $('#input-payment-code').val($('input[name=\'payment_method\']:checked').val());
 
                     $('#checkout-confirm').load('index.php?route=checkout/confirm.confirm&language={{ language }}');
@@ -150,82 +120,11 @@ $(document).on('submit', '#form-payment-method', function(e) {
     });
 });
 
-// Comment
-var timer = '';
-
-$('#input-comment').on('keydown', function() {
-    $('#button-confirm').prop('disabled', true);
-
-    // Request
-    clearTimeout(timer);
-
-    timer = setTimeout(function(object) {
-        chain.attach(function() {
-            return $.ajax({
-                url: 'index.php?route=checkout/payment_method.comment&language={{ language }}',
-                type: 'post',
-                data: $('#input-comment').serialize(),
-                dataType: 'json',
-                contentType: 'application/x-www-form-urlencoded',
-                success: function(json) {
-                    $('.alert-dismissible').remove();
-
-                    if (json['redirect']) {
-                        location = json['redirect'];
-                    }
-
-                    if (json['error']) {
-                        $('#alert').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa-solid fa-circle-exclamation"></i> ' + json['error'] + ' <button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>');
-
-                        $('#button-confirm').prop('disabled', false);
-                    }
-
-                    if (json['success']) {
-                        $('#alert').prepend('<div class="alert alert-success alert-dismissible"><i class="fa-solid fa-circle-check"></i> ' + json['success'] + ' <button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>');
-
-                        $('#button-confirm').prop('disabled', false);
-                    }
-
-                    window.setTimeout(function() {
-                        $('.alert-dismissible').fadeTo(1000, 0, function() {
-                            $(this).remove();
-                        });
-                    }, 3000);
-                },
-                error: function(xhr, ajaxOptions, thrownError) {
-                    $('#button-confirm').prop('disabled', false);
-
-                    console.log(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
-                }
-            });
-        });
-    }, 1000, this);
-});
-
-/* Agree to terms */
-$('#input-checkout-agree').on('change', function() {
-    var element = this;
-
-    chain.attach(function() {
-        return $.ajax({
-            url: 'index.php?route=checkout/payment_method.agree&language={{ language }}',
-            type: 'post',
-            data: $('#input-checkout-agree').serialize(),
-            dataType: 'json',
-            contentType: 'application/x-www-form-urlencoded',
-            beforeSend: function() {
-                $('#button-confirm').button('loading');
-            },
-            complete: function() {
-                $('#button-confirm').button('reset');
-            },
-            success: function(json) {
-                $('#checkout-confirm').load('index.php?route=checkout/confirm.confirm&language={{ language }}');
-            },
-            error: function(xhr, ajaxOptions, thrownError) {
-                console.log(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
-            }
-        });
-    });
-});
+if ({{ ready ? 'true' : 'false' }}) {
+    loadPaymentMethods();
+} else if (window.checkoutHasShipping) {
+    paymentMethodsPlaceholder('{{ text_select_shipping_first|escape('js') }}');
+} else {
+    paymentMethodsPlaceholder('{{ text_complete_info_first|escape('js') }}');
+}
 //--></script>
diff --git a/catalog/view/template/checkout/register.twig b/catalog/view/template/checkout/register.twig
index 62e10f9..a6c32ed 100644
--- a/catalog/view/template/checkout/register.twig
+++ b/catalog/view/template/checkout/register.twig
@@ -596,6 +596,13 @@ $('#form-register').on('submit', function(e) {
                     $('#input-shipping-method').val('');
                     $('#input-payment-method').val('');
 
+                    // If this cart doesn't need shipping, the payment method
+                    // list can be fetched right away - otherwise it waits
+                    // for a shipping method to be chosen first.
+                    if (typeof window.loadPaymentMethods === 'function' && !window.checkoutHasShipping) {
+                        window.loadPaymentMethods();
+                    }
+
                     $('#checkout-confirm').load('index.php?route=checkout/confirm.confirm&language={{ language }}');
                 }
             },
diff --git a/catalog/view/template/checkout/shipping_method.twig b/catalog/view/template/checkout/shipping_method.twig
index 3230c11..6b0ccba 100644
--- a/catalog/view/template/checkout/shipping_method.twig
+++ b/catalog/view/template/checkout/shipping_method.twig
@@ -132,6 +132,13 @@ $(document).on('submit', '#form-shipping-method', function(e) {
 
                     $('#input-payment-method').val('');
 
+                    // Now that a shipping method is picked, the payment
+                    // method list can be fetched (it depends on the shipping
+                    // zone, so it waits until now to load).
+                    if (typeof window.loadPaymentMethods === 'function') {
+                        window.loadPaymentMethods();
+                    }
+
                     $('#cart').load('index.php?route=common/cart.info&language={{ language }}');
                     $('#checkout-confirm').load('index.php?route=checkout/confirm.confirm&language={{ language }}');
                 }
