diff --git a/CHANGELOG.json b/CHANGELOG.json
index ad3a4ea..4cdd2e7 100644
--- a/CHANGELOG.json
+++ b/CHANGELOG.json
@@ -113,5 +113,10 @@
     "version": "5.6.8",
     "fa": "رفع باگ دکمه‌های + و - روی کارت محصول: در ۵.۶.۷ این دکمه‌ها به‌صورت یک ردیف جدا بالای دکمه «افزودن به سبد» اضافه شده بودند که با چیدمان قالب (چه قالب پیش‌فرض و چه قالب‌های سفارشی) تداخل داشت و باعث می‌شد روی برخی محصولات (بسته به طول توضیحات/اسم) دیده نشود یا روی دکمه افزودن به سبد بیفتد. حالا دکمه‌های + و - و عدد، دقیقاً کنار دکمه «افزودن به سبد» و داخل همان ردیف قرار گرفته‌اند (طبق درخواست) و این تداخل کاملاً برطرف شد. همچنین همین امکان + و - به خود صفحه سبد خرید هم اضافه شد تا بشود تعداد هر کالا را مستقیماً از آنجا هم تغییر داد. (یادآوری: افزایش تعداد با کلیک‌های پی‌درپی «افزودن به سبد» از قبل هم به‌درستی در سبد جمع می‌شد؛ این تغییر فقط نمایش/دسترسی آن را بهتر کرد.)",
     "en": "Fixed the product-card +/- quantity buttons: in 5.6.7 they were added as a separate row above the \"Add to Cart\" button, which clashed with the theme's layout (both the default theme and the custom ones) and could make them invisible or overlapping on some products depending on name/description length. They now sit directly beside the \"Add to Cart\" button, inside the same row, as requested - this fully resolves the layout conflict. The same +/- controls were also added to the cart page itself so quantities can be adjusted there too. (Note: quantity already correctly accumulated in the cart on repeated \"Add to Cart\" clicks before this fix - that part of the underlying logic was never broken, only the button's visibility/placement was.)"
+  },
+  {
+    "version": "5.6.9",
+    "fa": "امکان کم/زیاد کردن تعداد (+/-) به «سبد خرید کوچک» بالای سایت (که با کلیک روی آیکون سبد باز می‌شود) هم اضافه شد - قبلاً آنجا فقط عدد نمایش داده می‌شد و قابل تغییر نبود. همچنین در سبد خرید کوچک و در خود صفحه سبد خرید، تغییر تعداد (چه با دکمه‌های + و -، چه با تایپ مستقیم) دیگر نیاز به دکمه جداگانه «بروزرسانی» ندارد - دکمه‌ی رفرش/بروزرسانی حذف شد و به‌محض تغییر تعداد، مبلغ و جمع کل به‌صورت خودکار (با یک تاخیر کوتاه، برای اینکه چند کلیک پشت‌سرهم فقط یک درخواست بفرستد) به‌روز می‌شود.",
+    "en": "Added +/- quantity controls to the header mini-cart dropdown too - previously it only showed the quantity as plain text with no way to change it. Also, in both the mini-cart and the cart page itself, changing quantity (via +/- or by typing directly) no longer needs a separate \"update\" button - that refresh button was removed, and the total now updates on its own a moment after the change (debounced, so several quick +/- clicks only trigger one request)."
   }
 ]
\ No newline at end of file
diff --git a/VERSION b/VERSION
index 349e9d0..1502747 100644
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-5.6.8
+5.6.9
diff --git a/catalog/controller/common/cart.php b/catalog/controller/common/cart.php
index bf63c7e..26e4352 100644
--- a/catalog/controller/common/cart.php
+++ b/catalog/controller/common/cart.php
@@ -92,6 +92,7 @@ class Cart extends \MDcart\System\Engine\Controller {
 
 		$data['list'] = $this->url->link('common/cart.info', 'language=' . $this->config->get('config_language'));
 		$data['remove'] = $this->url->link('common/cart.remove', 'language=' . $this->config->get('config_language'));
+		$data['edit'] = $this->url->link('checkout/cart.edit', 'language=' . $this->config->get('config_language'));
 
 		$data['cart'] = $this->url->link('checkout/cart', 'language=' . $this->config->get('config_language'));
 		$data['checkout'] = $this->url->link('checkout/checkout', 'language=' . $this->config->get('config_language'));
diff --git a/catalog/view/javascript/common.js b/catalog/view/javascript/common.js
index 314002d..194ac02 100644
--- a/catalog/view/javascript/common.js
+++ b/catalog/view/javascript/common.js
@@ -482,7 +482,7 @@ $(document).on('click', '.qty-stepper-plus', function(e) {
         value = min;
     }
 
-    input.val(value + 1);
+    input.val(value + 1).trigger('change');
 });
 
 $(document).on('click', '.qty-stepper-minus', function(e) {
@@ -502,5 +502,30 @@ $(document).on('click', '.qty-stepper-minus', function(e) {
         value = min;
     }
 
-    input.val(value);
+    input.val(value).trigger('change');
+});
+
+// Auto-submit quantity changes for steppers marked ".qty-stepper-auto"
+// (the mini-cart dropdown and the cart page itself) so the total updates
+// on its own -- no separate "update"/refresh button needed there. Debounced
+// so a burst of +/- clicks collapses into one request. Relies on the form
+// having a real action="..." attribute (not just a submit button's
+// formaction), since there is no real submitter when triggered this way.
+$(document).on('change', '.qty-stepper-auto input[name=\'quantity\']', function() {
+    var input = this;
+    var min = parseInt($(input).attr('min'), 10) || 1;
+    var value = parseInt($(input).val(), 10);
+
+    if (isNaN(value) || value < min) {
+        value = min;
+        $(input).val(value);
+    }
+
+    window.clearTimeout($(input).data('qtyStepperTimer'));
+
+    var timer = window.setTimeout(function() {
+        $(input).closest('form').trigger('submit');
+    }, 400);
+
+    $(input).data('qtyStepperTimer', timer);
 });
diff --git a/catalog/view/template/checkout/cart.twig b/catalog/view/template/checkout/cart.twig
index 412023e..14ed60f 100644
--- a/catalog/view/template/checkout/cart.twig
+++ b/catalog/view/template/checkout/cart.twig
@@ -28,7 +28,7 @@ $('#shopping-cart').on('submit', '#output-cart form', function(e) {
     }
 
     $.ajax({
-        url: $(button).attr('formaction'),
+        url: $(button).attr('formaction') || $(element).attr('action'),
         type: 'post',
         data: $(element).serialize(),
         dataType: 'json',
diff --git a/catalog/view/template/checkout/cart_list.twig b/catalog/view/template/checkout/cart_list.twig
index 4feabdb..a4a855b 100644
--- a/catalog/view/template/checkout/cart_list.twig
+++ b/catalog/view/template/checkout/cart_list.twig
@@ -50,13 +50,12 @@
               </ul>
             </td>
             <td>
-              <form method="post" data-oc-target="#shopping-cart">
-                <div class="input-group qty-stepper">
+              <form method="post" action="{{ edit }}" data-oc-target="#shopping-cart">
+                <div class="input-group qty-stepper qty-stepper-auto">
                   <button type="button" class="btn btn-light qty-stepper-minus" tabindex="-1"><i class="fa-solid fa-minus"></i></button>
                   <input type="text" name="quantity" value="{{ product.quantity }}" min="1" size="1" inputmode="numeric" class="form-control text-center{% if product.minimum %} is-invalid{% endif %}"/>
                   <button type="button" class="btn btn-light qty-stepper-plus" tabindex="-1"><i class="fa-solid fa-plus"></i></button>
                   <input type="hidden" name="key" value="{{ product.cart_id }}"/>
-                  <button type="submit" formaction="{{ edit }}" data-bs-toggle="tooltip" title="{{ button_update }}" class="btn btn-primary"><i class="fa-solid fa-rotate"></i></button>
                   <a href="{{ product.remove }}" data-bs-toggle="tooltip" title="{{ button_remove }}" class="btn btn-danger"><i class="fa-solid fa-circle-xmark"></i></a>
                 </div>
               </form>
diff --git a/catalog/view/template/common/cart.twig b/catalog/view/template/common/cart.twig
index 347693b..9a9be89 100644
--- a/catalog/view/template/common/cart.twig
+++ b/catalog/view/template/common/cart.twig
@@ -29,7 +29,16 @@
                   {% endif %}
                 </ul>
               </td>
-              <td class="text-end text-nowrap">x {{ product.quantity }}</td>
+              <td class="text-nowrap">
+                <form action="{{ edit }}" method="post" data-oc-toggle="ajax" data-oc-load="{{ list }}" data-oc-target="#cart">
+                  <div class="input-group input-group-sm qty-stepper qty-stepper-auto">
+                    <button type="button" class="btn btn-light qty-stepper-minus" tabindex="-1"><i class="fa-solid fa-minus"></i></button>
+                    <input type="text" name="quantity" value="{{ product.quantity }}" min="1" size="1" inputmode="numeric" class="form-control text-center"/>
+                    <button type="button" class="btn btn-light qty-stepper-plus" tabindex="-1"><i class="fa-solid fa-plus"></i></button>
+                  </div>
+                  <input type="hidden" name="key" value="{{ product.cart_id }}">
+                </form>
+              </td>
               <td class="text-end">{{ product.total }}</td>
               <td class="text-end">
                 <form action="{{ remove }}" method="post" data-oc-toggle="ajax" data-oc-load="{{ list }}" data-oc-target="#cart">
