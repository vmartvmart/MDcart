diff --git a/CHANGELOG.json b/CHANGELOG.json
index 22f6bfc..de714f2 100644
--- a/CHANGELOG.json
+++ b/CHANGELOG.json
@@ -208,5 +208,10 @@
     "version": "5.6.27",
     "fa": "دو درگاه پرداخت جدید «آقای پرداخت» و «بیت‌پی» به بسته‌ی درگاه‌های پرداخت ایرانی اضافه شدند (در کنار زرین‌پال، آی‌دی‌پی، پی‌پینگ، سیزپی و کارت‌به‌کارت). پس از این بروزرسانی، به «افزونه‌ها > درگاه‌های پرداخت» بروید و کد پین (آقای پرداخت) یا کلید API (بیت‌پی) خودتان را وارد کنید تا فعال شوند.",
     "en": "Added two new payment gateways, Aghaye Pardakht and BitPay, to the Iranian Payment Gateways extension package (alongside ZarinPal, IDPay, PayPing, SizPay and card-to-card). After this update, go to Extensions > Payment Gateways and enter your own Pin code (Aghaye Pardakht) or API key (BitPay) to activate them."
+  },
+  {
+    "version": "5.6.28",
+    "fa": "امکان «حالت تست (Sandbox)» به درگاه بیت‌پی اضافه شد تا بشود قبل از وارد کردن اطلاعات واقعی، کل مسیر پرداخت را با محیط تستِ رسمی بیت‌پی امتحان کرد — بدون جابه‌جایی پول واقعی. برای تست: در «افزونه‌ها > درگاه‌های پرداخت > بیت‌پی»، کلید API تستی که بیت‌پی در مستندات رسمی خودش منتشر کرده را در فیلد «کلید API» وارد کنید و «حالت تست» را روشن کنید، سپس یک سفارش آزمایشی ثبت کنید. پیش از فعال شدن واقعی سایت، حتماً کلید API واقعی خودتان را جایگزین کرده و «حالت تست» را خاموش کنید.",
+    "en": "Added a Sandbox (test) mode to the BitPay gateway so the full checkout flow can be tried against BitPay's own official test environment before entering real credentials - no real money moves. To test: under Extensions > Payment Gateways > BitPay, enter the test API key BitPay publishes in its own official documentation into the API Key field, turn on Sandbox Mode, then place a test order. Before going live, replace it with your real API key and turn Sandbox Mode back off."
   }
 ]
diff --git a/VERSION b/VERSION
index 40b7fe9..030ab30 100644
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-5.6.27
+5.6.28
\ No newline at end of file
diff --git a/extension/iranian_gateways/admin/controller/payment/bitpay.php b/extension/iranian_gateways/admin/controller/payment/bitpay.php
index a1583d0..90ffc69 100644
--- a/extension/iranian_gateways/admin/controller/payment/bitpay.php
+++ b/extension/iranian_gateways/admin/controller/payment/bitpay.php
@@ -42,6 +42,7 @@ class Bitpay extends \MDcart\System\Engine\Controller {
 		$data['back'] = $this->url->link('marketplace/extension', 'user_token=' . $this->session->data['user_token'] . '&type=payment');
 
 		$data['payment_bitpay_api'] = $this->config->get('payment_bitpay_api');
+		$data['payment_bitpay_sandbox'] = $this->config->get('payment_bitpay_sandbox');
 
 		// Order Status
 		$data['payment_bitpay_order_status_id'] = (int)$this->config->get('payment_bitpay_order_status_id');
diff --git a/extension/iranian_gateways/admin/language/fa/payment/bitpay.php b/extension/iranian_gateways/admin/language/fa/payment/bitpay.php
index b3e82ed..fdd65c5 100644
--- a/extension/iranian_gateways/admin/language/fa/payment/bitpay.php
+++ b/extension/iranian_gateways/admin/language/fa/payment/bitpay.php
@@ -11,13 +11,15 @@ $_['text_callback_url']   = 'آدرس بازگشت (Callback URL)';
 
 // Entry
 $_['entry_api']           = 'کلید API';
+$_['entry_sandbox']       = 'حالت تست (Sandbox)';
 $_['entry_order_status']  = 'وضعیت سفارش (پرداخت موفق)';
 $_['entry_geo_zone']      = 'منطقه جغرافیایی';
 $_['entry_status']        = 'وضعیت';
 $_['entry_sort_order']    = 'ترتیب نمایش';
 
 // Help
-$_['help_api']             = 'کلید API دریافتی از پنل بیت‌پی (bitpay.ir).';
+$_['help_api']             = 'کلید API دریافتی از پنل بیت‌پی (bitpay.ir). برای تست، می‌توانید کلید تست بیت‌پی را وارد کرده و «حالت تست» را روشن کنید.';
+$_['help_sandbox']         = 'استفاده از محیط تست بیت‌پی برای آزمایش درگاه — پول واقعی جابه‌جا نمی‌شود. قبل از فعال شدن واقعی سایت، این گزینه را خاموش کنید.';
 $_['help_callback_url']    = 'اگر پنل بیت‌پی آدرس بازگشت (Redirect) درخواست کرد، از این آدرس استفاده کنید.';
 
 // Error
diff --git a/extension/iranian_gateways/admin/language/us/payment/bitpay.php b/extension/iranian_gateways/admin/language/us/payment/bitpay.php
index eba066b..ab2b37f 100644
--- a/extension/iranian_gateways/admin/language/us/payment/bitpay.php
+++ b/extension/iranian_gateways/admin/language/us/payment/bitpay.php
@@ -11,13 +11,15 @@ $_['text_callback_url']   = 'Callback URL';
 
 // Entry
 $_['entry_api']           = 'API Key';
+$_['entry_sandbox']       = 'Sandbox Mode';
 $_['entry_order_status']  = 'Order Status (Successful Payment)';
 $_['entry_geo_zone']      = 'Geo Zone';
 $_['entry_status']        = 'Status';
 $_['entry_sort_order']    = 'Sort Order';
 
 // Help
-$_['help_api']             = 'The API Key from your BitPay panel (bitpay.ir).';
+$_['help_api']             = 'The API Key from your BitPay panel (bitpay.ir). For testing, you can enter BitPay\'s test API key and turn on Sandbox Mode.';
+$_['help_sandbox']         = 'Use BitPay\'s test/sandbox environment - no real money changes hands. Turn off before going live.';
 $_['help_callback_url']    = 'If your BitPay panel asks you to set a redirect/callback URL, use this address.';
 
 // Error
diff --git a/extension/iranian_gateways/admin/view/template/payment/bitpay.twig b/extension/iranian_gateways/admin/view/template/payment/bitpay.twig
index bb308c9..c16148f 100644
--- a/extension/iranian_gateways/admin/view/template/payment/bitpay.twig
+++ b/extension/iranian_gateways/admin/view/template/payment/bitpay.twig
@@ -26,6 +26,16 @@
               <div class="form-text">{{ help_api }}</div>
             </div>
           </div>
+          <div class="row mb-3">
+            <label class="col-sm-2 col-form-label">{{ entry_sandbox }}</label>
+            <div class="col-sm-10">
+              <div class="form-check form-switch form-switch-lg">
+                <input type="hidden" name="payment_bitpay_sandbox" value="0"/>
+                <input type="checkbox" name="payment_bitpay_sandbox" value="1" id="input-sandbox" class="form-check-input"{% if payment_bitpay_sandbox %} checked{% endif %}/>
+              </div>
+              <div class="form-text">{{ help_sandbox }}</div>
+            </div>
+          </div>
           <div class="row mb-3">
             <label class="col-sm-2 col-form-label">{{ text_callback_url }}</label>
             <div class="col-sm-10">
diff --git a/extension/iranian_gateways/catalog/controller/payment/bitpay.php b/extension/iranian_gateways/catalog/controller/payment/bitpay.php
index 3868a68..2a119d8 100644
--- a/extension/iranian_gateways/catalog/controller/payment/bitpay.php
+++ b/extension/iranian_gateways/catalog/controller/payment/bitpay.php
@@ -11,6 +11,13 @@ class Bitpay extends \MDcart\System\Engine\Controller {
 	private const GATE_URL_PREFIX = 'https://bitpay.ir/payment/gateway-';
 	private const GATE_URL_SUFFIX = '-get';
 
+	// BitPay's own test/sandbox environment - same shape as production, just
+	// a different URL prefix ("payment-test" instead of "payment"), per
+	// BitPay's official test-environment documentation.
+	private const API_SEND_URL_SANDBOX = 'https://bitpay.ir/payment-test/gateway-send';
+	private const API_VERIFY_URL_SANDBOX = 'https://bitpay.ir/payment-test/gateway-result-second';
+	private const GATE_URL_PREFIX_SANDBOX = 'https://bitpay.ir/payment-test/gateway-';
+
 	/**
 	 * Index
 	 *
@@ -43,6 +50,8 @@ class Bitpay extends \MDcart\System\Engine\Controller {
 			$amount = $this->getRialAmount($order_info, $json);
 
 			if (!$json) {
+				$sandbox = (bool)$this->config->get('payment_bitpay_sandbox');
+
 				$fields = [
 					'api'         => $this->config->get('payment_bitpay_api'),
 					'amount'      => $amount,
@@ -53,14 +62,16 @@ class Bitpay extends \MDcart\System\Engine\Controller {
 					'description' => sprintf($this->language->get('text_order_description'), $order_info['order_id'], $this->config->get('config_name'))
 				];
 
-				[$id_get, $error] = $this->requestIdGet($fields);
+				[$id_get, $error] = $this->requestIdGet($fields, $sandbox);
 
 				if ($error || $id_get === null) {
 					$json['error'] = $this->language->get('error_gateway');
 				} else {
 					$this->session->data['bitpay_id_get'] = $id_get;
 
-					$json['redirect'] = self::GATE_URL_PREFIX . $id_get . self::GATE_URL_SUFFIX;
+					$gate_prefix = $sandbox ? self::GATE_URL_PREFIX_SANDBOX : self::GATE_URL_PREFIX;
+
+					$json['redirect'] = $gate_prefix . $id_get . self::GATE_URL_SUFFIX;
 				}
 			}
 		}
@@ -111,6 +122,8 @@ class Bitpay extends \MDcart\System\Engine\Controller {
 			return;
 		}
 
+		$sandbox = (bool)$this->config->get('payment_bitpay_sandbox');
+
 		$fields = [
 			'api'      => $this->config->get('payment_bitpay_api'),
 			'id_get'   => $id_get,
@@ -118,7 +131,7 @@ class Bitpay extends \MDcart\System\Engine\Controller {
 			'json'     => 1
 		];
 
-		[$result, $error] = $this->curlPostForm(self::API_VERIFY_URL, $fields);
+		[$result, $error] = $this->curlPostForm($sandbox ? self::API_VERIFY_URL_SANDBOX : self::API_VERIFY_URL, $fields);
 
 		$status = $result['status'] ?? null;
 
@@ -155,11 +168,12 @@ class Bitpay extends \MDcart\System\Engine\Controller {
 	 * both shapes are handled defensively.
 	 *
 	 * @param array<string, mixed> $fields
+	 * @param bool                 $sandbox
 	 *
 	 * @return array{0: string|null, 1: bool}
 	 */
-	private function requestIdGet(array $fields): array {
-		[$response, $error] = $this->curlPostForm(self::API_SEND_URL, $fields, false);
+	private function requestIdGet(array $fields, bool $sandbox = false): array {
+		[$response, $error] = $this->curlPostForm($sandbox ? self::API_SEND_URL_SANDBOX : self::API_SEND_URL, $fields, false);
 
 		if ($error || $response === null) {
 			return [null, true];
